// Criando um array para armazenar as cartas
const cartasYuGiOh = [];

// Função auxiliar para criar uma nova carta
function criarCarta(nome, tipo, efeito, linguagem) {
  return {
    nome,
    tipo,
    efeito,
    linguagem,
  };
}

// Criando as cartas

// Monstros
cartasYuGiOh.push(
  criarCarta("Dragão JavaScript", "Monstro", "Duplica o ataque de todos os monstros com o tipo 'Função'.", "JavaScript"),
  criarCarta("Mago Python", "Monstro", "Inflige 500 pontos de dano ao oponente por cada módulo instalado.", "Python"),
  criarCarta("Guerreiro C++", "Monstro", "Aumenta o próprio ataque em 1000 pontos por cada ponteiro alocado.", "C++")
);

// Magias
cartasYuGiOh.push(
  criarCarta("Encantamento Recursivo", "Magia", "Invoca uma cópia deste card para cada nível de recursão.", "Todas"),
  criarCarta("Butim da Biblioteca", "Magia", "Adicione um card aleatório de sua biblioteca à sua mão.", "Todas"),
  criarCarta("Depuração", "Magia", "Revele a mão do oponente e escolha um card para banir.", "Todas")
);

// Armadilhas
cartasYuGiOh.push(
  criarCarta("Erro 404", "Armadilha", "Nega o efeito de qualquer card do oponente.", "Todas"),
  // ... outras cartas com efeitos relacionados a erros comuns em programação
);

// Exibindo as cartas
console.log(cartasYuGiOh);